<h3>My Bookmarks</h3>

<div class="row">
  <div class="col-md-8">
    <% @user_bookmarks.each do |topic, bookmarks| %>
      <h4><%= link_to topic.title, topic_path(topic) %></h4>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <!-- Loop through bookmark information -->
            <!-- Able to click on link, edit, like and delete -->
            <!-- <%# .each do %>

            <%# end %> -->
          </h4>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">
    <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
  </div>
</div>

<h3>My Liked Bookmarks</h3>

<div class="row">
  <div class="col-md-8">
    <% @liked_bookmarks.each do |topic, bookmarks| %>
      <h4><%= link_to topic.title, topic_path(topic) %></h4>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <!-- Loop through bookmark information -->
            <!-- Able to click on link, edit, like and delete -->
            <!-- <%# .each do %>

            <%# end %> -->
          </h4>
        </div>
      </div>
    <% end %>
  </div>
</div>
