%h1 Edit bookmark

.row
  .col-md-8
    = form_for [@bookmark.topic, @bookmark] do |f|
      .form-group
        = f.label :url
        = f.txt_field :url, class: 'form-control', placeholder: "Enter bookmark url."
      .form-group
        = f.submit "Save", class: 'btn btn-success'
